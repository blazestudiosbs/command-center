import WorkspacePlaceholder from "../../components/WorkspacePlaceholder";

export default function SettingsPage() {
  return <WorkspacePlaceholder title="Settings" subtitle="Command Center preferences and platform configuration." description="Shared application settings will move here as configurable options are introduced." />;
}
